<h1><%= link_to @song.title, song_path(@song) %></h1>

<p>by  <%= display_artist(@song)%> (<%= link_to "Edit Song", edit_artist_song_path(@song.artist, @song) if @song.artist %>)</p>

<%= link_to "All Songs", songs_path %>
